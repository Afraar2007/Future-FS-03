{
  "name": "future-fs-03",
  "private": true,
  "type": "module",
  "workspaces": ["orderly-eats"],
  "scripts": {
    "dev": "npm --workspace orderly-eats run dev",
    "build": "npm --workspace orderly-eats run build",
    "preview": "npm --workspace orderly-eats run preview",
    "vercel-build": "npm --workspace orderly-eats run build"
  },
  "dependencies": {
    "mongodb": "^7.3.0",
    "mongoose": "^8.0.0"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  }
}
